<%= f.govuk_collection_radio_buttons :subject, @form.class::OPTIONS, :first, :second, legend: { text: t("helpers.legend.request_form.form_request_subject"), tag: "h1" } %>
<%= f.hidden_field :default %>
